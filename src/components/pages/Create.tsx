import "../../index.scss";
import Header from "../header/Header";

const create = () => {
	return (
		<div className="create">
			<Header homepage={false} />
			<main></main>
		</div>
	);
};

export default create;
